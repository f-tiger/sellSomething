rootProject.name = "llmstxt-agentsmd-support"

plugins {
    // Auto-provisions a JDK 21 toolchain when none is installed locally.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}
