name: Build MCP Server Index

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1" # weekly, Monday 06:00 UTC

permissions:
  contents: write

jobs:
  build-index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: claude/sales-website-research-plan-7i7rc2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Scan public MCP servers and build index data
        run: node scripts/build-mcp-index.mjs
      - name: Commit updated index
        run: |
          git config user.name "mcp-index-bot"
          git config user.email "actions@github.com"
          git add sites/mcppulse/public/data/mcp-index.json
          git diff --cached --quiet && echo "No changes" || git commit -m "chore: refresh Public MCP Server Index data"
          for i in 1 2 3 4; do
            git push && break
            git pull --rebase && continue || { git rebase --abort 2>/dev/null; sleep $((i*2)); git pull --rebase; }
          done
      # GITHUB_TOKEN pushes don't retrigger deploy, so deploy mcppulse directly.
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: sites/mcppulse
          wranglerVersion: "4.112.0"
